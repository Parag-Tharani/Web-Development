import { createTheme } from "@mui/material";


const Theme = createTheme({
    palette:{
        mode:'dark',
        primary:{
            main: 'rgb(125,75,20)'
        }
    }
})

export default Theme